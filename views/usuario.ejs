<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detalles del Usuario</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #6a11cb, #2575fc);
            color: #fff;
            font-family: 'Arial', sans-serif;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .card {
            background: rgba(255, 255, 255, 0.1);
            padding: 20px;
            border-radius: 15px;
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.2);
            animation: fadeIn 1.5s ease-in-out;
            width: 50%;
        }
        .card h1 {
            text-align: center;
            margin-bottom: 20px;
            font-weight: bold;
        }
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>
    <div class="card">
        <h1>Detalles del Usuario</h1>
        <p><strong>ID:</strong> <%= usuario.id %></p>
        <p><strong>Nombre:</strong> <%= usuario.nombre %></p>
        <p><strong>Correo:</strong> <%= usuario.correo %></p>
        <a href="/usuarios" class="btn btn-secondary w-100 mt-3">Volver a la Lista</a>
    </div>
    <h2>Resultados de Tests</h2>
<% if (resultados && resultados.length > 0) { %>
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Tipo de Test</th>
                <th>Resultado</th>
                <th>Porcentaje</th>
                <th>Fecha</th>
                <th>Ver Respuestas</th>
            </tr>
        </thead>
        <tbody>
            <% resultados.forEach(r => { %>
                <tr>
                    <td><%= r.tipo_test %></td>
                    <td><%= r.resultado %></td>
                    <td><%= r.porcentaje %> %</td>
                    <td><%= r.fecha.toLocaleString() %></td>
                    <td>
                        <button class="btn btn-info btn-sm" onclick='alert(JSON.stringify(<%- r.respuestas %>, null, 2))'>Ver</button>
                    </td>
                </tr>
            <% }) %>
        </tbody>
    </table>
<% } else { %>
    <p>No hay resultados de tests para este usuario.</p>
<% } %>
</body>
</html>